interface String {
    /**
     * Returns a new string with its characters in reverse order.
     * 
     * @example
     * var foo = 'abcde';
     * console.log(foo.reverse()); // edcba
     */
    reverse(): string;
}